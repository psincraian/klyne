// Main JavaScript entry point for common functionality across all pages
// Fonts and CSS are now imported via CSS files, not JS

// Common utilities and functions that are used across multiple pages
console.log('Klyne application initialized');